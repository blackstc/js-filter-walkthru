// Utility functions

// function getValues(arr) {
//   var results = arr.filter(smaller);
//   return results;
// }

// function inRange(value, upper, lower) {
//   return (value <= upper && value >= lower);
// }
